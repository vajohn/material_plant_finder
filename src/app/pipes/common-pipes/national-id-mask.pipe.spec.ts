import { NationalIdMaskPipe } from './national-id-mask.pipe';

describe('NationalIdMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalIdMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
